const prompt = require("prompt-sync")({ sigint: true });

function pulgadaCentimetro (x) {
        return x * 25.4
     let resultado = pulgadaCentimetro
}
pulgadaCentimetro (prompt ("Numero expresado en pulgadas:"))

console.log (resultado)

